<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.gravitydevelopment.cnu"
    android:installLocation="internalOnly" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".CNU"
            android:label="@string/title_activity_cnudining"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CNUSettings"
            android:label="@string/settings_name"
            android:parentActivityName=".CNU" >
        </activity>

        <receiver android:name=".receiver.BootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name=".service.BackendService" />

        <activity
            android:name=".CNUViewLocation"
            android:label="@string/title_activity_cnuview_location"
            android:parentActivityName=".CNU" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.gravitydevelopment.cnu.CNU" />
        </activity>
    </application>

</manifest>
